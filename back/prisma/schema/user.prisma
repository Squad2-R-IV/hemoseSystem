model User {
  id            String @id @default(uuid())
  name          String
  password      String
  email         String @unique
  cpf           String @unique
  contato       String
  status        String @default("A")
  especialidade String?
  conselho      String?
  registro      String?
  refreshToken  String?
  roles         UserToRole[]
}
